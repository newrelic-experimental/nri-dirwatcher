name: com.newrelic.dirwatcher
description: Reports metrics about files and directories
protocol_version: 2
os: linux

commands:
  metrics:
    command:
      - ./nri-dirwatcher/nri-dirwatcher
    interval: 30
